1.存取字串中的字元：
char ch = str[0]; // 存取第一個字元

2.取得字串長度：
int len = str.length(); // 取得字串長度

3.字串相加：
string result = str1 + str2; // 將兩個字串相加

4.字串比較：
if (str1 == str2) {
    // 字串相等
}

5.使用 getline() 讀取一整行字串：
getline(std::cin, str); // 讀取一整行字串，包括空白字元

